# Android APK Downloads

Place the signed Android APK here as `agorax.apk`.

The file will be served at `/api/android/download` (authenticated users only).

Example:
```
downloads/
  agorax.apk   ← place the signed release APK here
```
